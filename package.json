{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Personal portfolio website showcasing skills & projects",
  "main": "index.html",
  "scripts": {
    "dev": "live-server --port=8000 --open=/ --watch=index.html,styles.css,script.js,assets",
    "start": "npm run dev",
    "clean": "rm -rf .cache dist node_modules/.cache",
    "deploy": "echo '✅ Portfolio is ready for deployment!' && echo '' && echo '📦 Deployment Options:' && echo '  1. GitHub Pages: git push origin main' && echo '  2. Netlify: Drag & drop this folder to netlify.com' && echo '  3. Vercel: vercel --prod' && echo '' && echo '🌐 Current files ready to deploy:' && ls -1 *.html *.css *.js 2>/dev/null | head -5"
  },
  "keywords": [
    "portfolio",
    "website",
    "developer",
    "fullstack"
  ],
  "author": "Asish Nelapati",
  "license": "MIT",
  "devDependencies": {
    "live-server": "^1.2.2",
    "tailwindcss": "^4.0.8"
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  }
}
